export function makeCountString(waterCount, skylineCount, castleCount) {
    return `You've changed the waterfront ${waterCount} times, the skyline ${skylineCount} times, and the castle ${castleCount} times.`;
}